#include<stdio.h>

int main(){
  int x,y,d,i;
  scanf("%d%d",&x,&y);
  if(x>=y){
    d=y;
  }
  else {
    d=x;
  }
  for(i=d;i>=0;i--){
    if(x%i==0&&y%i==0){
      printf("%d\n",i);
      break;
    }
  }
  return 0;
}
