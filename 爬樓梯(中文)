#include<stdio.h>

int f(int n){
  if(n==1||n==2){
    return n;
  }
  else {
    return f(n-1)+f(n-2);
  }
}

int main(){
  int num,a;
  while(scanf("%d",&num)!=EOF){
    printf("%d\n",f(num));
  }
  return 0;
}
