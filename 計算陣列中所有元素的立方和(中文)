#include<iostream>
using namespace std;

int a[1000],i,sum=0;

int main(){
  for(i=0;i<6;i++){
    cin >> a[i];
  }
  for(i=0;i<6;i++){
    sum+=(a[i]*a[i]*a[i]);
  }
  cout << sum << endl;
  return 0;
}
